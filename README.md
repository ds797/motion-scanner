# motion-scanner: Rust API bindings for the Wayland protocol

## Setup
Use `cargo new --lib output` to create the output library. Once you've done this, simply execute `cargo run` to generate bindings. The input/output paths can be modified in `main.rs`.
